# Copy this file to .streamlit/secrets.toml and fill in your key.
# On Streamlit Community Cloud, add these under App settings → Secrets instead.
# NEVER commit secrets.toml itself (it is in .gitignore).

NEWSAPI_KEY = "your_newsapi_key_here"

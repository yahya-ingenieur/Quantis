# Quantis dependencies
#
# Local setup (Windows):
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Streamlit Cloud: install happens automatically from this file.
#
# Run the pipeline:
#   python src/data.py
#   python -m src.train
#   python -m src.evaluate
#   streamlit run src/app.py

# --- core data / modelling ---
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0

# PyTorch — CPU build (sufficient for inference; no GPU needed)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# --- evaluation / plots ---
matplotlib>=3.7.0
scipy>=1.10.0

# --- demo app ---
streamlit>=1.30.0
plotly>=5.0.0

# --- news sentiment (app degrades gracefully if these fail to load) ---
transformers>=4.30.0
vaderSentiment>=3.3.2
requests>=2.28.0

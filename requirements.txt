# Quantis dependencies — pinned for reproducibility.
#
# Tested with Python 3.11. Set up a clean, isolated environment so a grader can
# reproduce the exact results:
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   |   source .venv/bin/activate (macOS/Linux)
#   pip install -r requirements.txt
#
# Then run the pipeline:
#   python src/data.py            # download + build dataset
#   python -m src.train           # train LSTM/GRU, baselines, experiments
#   python -m src.evaluate        # metrics, backtest, plots
#   python -m src.report_plots    # extra report figures
#   streamlit run src/app.py      # live demo
#
# Optional (news sentiment side-experiment):
#   python -m src.sentiment fetch        # pull headlines (FNSPID, Hugging Face)
#   python -m src.sentiment score        # FinBERT + VADER daily scores
#   python -m src.sentiment_experiment   # with-vs-without sentiment
#   python -m src.sentiment live-compare # live FinBERT-vs-VADER (needs NEWSAPI_KEY in .env)
#   python -m src.robustness             # multi-seed LSTM vs GRU

# --- core data / modelling ---
yfinance==1.4.1
pandas==2.2.3
numpy==1.26.4
torch==2.12.0          # tested on the CPU build (2.12.0+cpu); a CPU install is sufficient
scikit-learn==1.8.0

# --- evaluation / plots ---
matplotlib==3.10.8
scipy==1.16.3

# --- demo app ---
streamlit==1.58.0
plotly==6.8.0

# --- news sentiment side-experiment ---
transformers==5.12.1     # FinBERT scoring
vaderSentiment==3.3.2    # VADER baseline scoring
requests==2.32.3         # NewsAPI calls
# (huggingface_hub is pulled in by transformers)
